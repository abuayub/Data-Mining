KNN Code:
1. It is a python based code.
2. Installation of Python 3.4.3 is required(I have used Anaconda 2.3 |Spyder 2 Editor)
3. In python shell use command: python knn_kfold.py
4. Keep csv files in same folder.

Running:
1. Enter data filename (default is iris.csv).
2. Enter K value for KNN (default is iris.csv).
3. K value for kfold is fixed to 5